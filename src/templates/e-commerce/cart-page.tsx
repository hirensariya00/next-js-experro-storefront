/* eslint-disable*/
// TODO: Disabled eslint due to component is not used anywhere
export interface CartPageProps {
  pageData: any;
  components: any;
}

const CartPage = ({}: CartPageProps) => {
  return <div>{/* <Cart pageData={pageData}/> */}</div>;
};

export default CartPage;
